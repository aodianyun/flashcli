# Bench script tests

Unit tests for `scripts/bench_*.py` (Qwen HTTP benchmark tooling). Not run in the default CI / `pytest tests/` suite.

```bash
pytest tests/bench/
```

See `scripts/README.bench_qwen36.md` for the live benchmark workflow.
